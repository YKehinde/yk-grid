// Stub — implemented in phase 5 (grouping + aggregation).
export type AggregationType = 'sum' | 'avg' | 'count' | 'min' | 'max'

export function aggregate(values: number[], type: AggregationType): number {
  void values
  void type
  return 0
}
