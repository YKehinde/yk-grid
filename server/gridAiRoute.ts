// Example proxy route — adapt to Express/Next/Fastify in phase 11.
// The key never leaves the server. The client swaps LLM_API_KEY + optionally LLM_PROVIDER.

// import { AnthropicProvider } from './providers/anthropic'
// import { OpenAiProvider } from './providers/openai'
// import { AiCommandSchema } from '../src/components/DataGrid/ai/schema'

// provider selection (phase 11):
// const provider = process.env.LLM_PROVIDER === 'openai'
//   ? new OpenAiProvider()
//   : new AnthropicProvider()

export {}
